let product = {
    Name: "Stanza Mansion",
    Color: "Beige",
    Location: "xyz road"
}
console.log(product)

let JSON_product = JSON.stringify(product)
console.log(JSON_product)

let JSON_rewind = JSON.parse(JSON_product)
console.log(JSON_rewind)
